# Codebase Summary

> Auto-generated by /summarize on 2026-04-28

## Project Overview

- **Name:** cabinet-layout
- **Description:** Browser-based 2D electrical cabinet layout tool — drag, arrange, and annotate DIN-rail components on a zoomable SVG canvas, then export as SVG/PNG/JPG
- **Status:** Active development
- **Key features:**
  - Drag-and-drop component palette onto an infinite SVG canvas
  - Wireduct drawing (horizontal/vertical), rectangle annotation, free text labels
  - Per-instance part-number toggle (right-click context menu)
  - Rubber-band multi-select; move/rotate selected items
  - Undo history; snap-to-grid
  - CSV import (replace or append mode) with preview dialog
  - Export image: SVG, PNG, JPG (grid excluded, auto-zoom-to-fit content)
  - Zoom 10–10 000 %; pan via middle-mouse or Space+drag
  - Text scale and line thickness sliders (0–10×, zoom-independent)
  - Palette CRUD: add, edit dimensions/qty, delete with confirm

## Technology Stack

| Category | Technology |
|----------|-----------|
| Language | TypeScript 5.5 |
| Framework | React 18 |
| Rendering | SVG (no canvas element) |
| Build tool | Vite 5 |
| Testing | Vitest 2 |
| Package manager | npm |
| Deployment | Static (vite build) |

## Project Structure

```
C:\AI\Layout\
├── index.html              Entry HTML
├── vite.config.ts          Vite + React plugin config
├── tsconfig.json           Strict TS config
├── package.json
├── src/
│   ├── main.tsx            React root mount
│   ├── App.tsx             Single top-level component; all state + handlers
│   ├── App.css             All application styles
│   ├── types.ts            Shared TypeScript interfaces
│   ├── components/
│   │   ├── Canvas.tsx          SVG canvas (forwardRef); renders all items
│   │   ├── PlacedComponent.tsx  Single placed component rect+label
│   │   ├── Wireduct.tsx         Wireduct SVG element
│   │   ├── Toolbar.tsx          Top toolbar: tools, zoom, sliders, export
│   │   ├── Palette.tsx          Left panel: component list, CRUD, CSV load
│   │   └── CsvPreviewDialog.tsx CSV import preview + delimiter + mode
│   └── utils/
│       ├── csv.ts / csv.test.ts        CSV parser with auto-detect delimiter
│       ├── overlap.ts / overlap.test.ts AABB overlap detection
│       ├── zoom.ts / zoom.test.ts       Zoom helpers
│       ├── storage.ts                   localStorage save/load
│       └── dxf.ts                       DXF exporter (unused in UI, kept)
└── docs/
    ├── codebase-summary.md
    └── superpowers/
        ├── plans/
        └── specs/
```

## Key Components

| File | Role |
|------|------|
| `src/App.tsx` | All application state (placed items, wireducts, rects, texts, history, drag, tool, zoom, grid, export, context menu). Passes callbacks down. |
| `src/components/Canvas.tsx` | `forwardRef` SVG element. Renders grid pattern, wireducts, rects, rect-preview, selection box, placed components, text items. Exposes `getSvgRect` / `getSvgElement`. |
| `src/components/Toolbar.tsx` | Tool buttons (select/wireduct/rect/rotate/text), zoom slider (10–10 000 %), text scale + line scale sliders, export dropdown (SVG/PNG/JPG), undo/redo, grid toggle. |
| `src/components/Palette.tsx` | Component list cards with inline edit, delete confirm modal, add-item form with auto-ID. Drag-source for canvas drop. CSV file load button. |
| `src/components/CsvPreviewDialog.tsx` | Modal dialog: delimiter selector, replace/append mode, preview table with invalid-row highlighting, confirm/cancel. |
| `src/utils/csv.ts` | `parseCsv(text, delimiter)` + `detectDelimiter(line)` |
| `src/utils/overlap.ts` | AABB collision detection for overlap highlighting |
| `src/utils/storage.ts` | `saveLayout` / `loadLayout` via localStorage |
| `src/utils/zoom.ts` | `clampZoom`, `zoomToPoint` helpers |

## Core Data Types (`src/types.ts`)

```ts
ComponentDef    { id, partNumber, width, height, depth, qty }
PlacedComponent { instanceId, defId, x, y, rotation, showPN? }
Wireduct        { id, x, y, length, orientation, ductWidth }
TextItem        { id, x, y, text, fontSize }
DrawnRect       { id, x, y, width, height }
Layout          { components, wireducts, rects, texts? }
GridSettings    { size, visible }
ViewTransform   { scale, offsetX, offsetY }
Tool            'select' | 'wireduct' | 'rect' | 'rotate' | 'text'
```

## Key Patterns

- `textUnit = textScale / scale` — font size stays visually stable across zoom, but user-scalable via slider
- `strokeUnit = lineScale / scale` — stroke width likewise
- Export: SVG viewBox set to screen-space content bounds; group transform kept intact — no DOM font-size manipulation needed
- `startDrag` unified useCallback collects all four draggable types (placed, text, rect, wireduct) before any mousemove
- Undo: `pushHistory()` snapshots state before any mutation; single `history` stack, no redo

## Statistics

| Metric | Value |
|--------|-------|
| Total files | 30 |
| Total tokens (repomix) | 50,120 |
| Source files | 14 (src/) |
| Test files | 3 |
| Utility modules | 5 |
| Last updated | 2026-04-28 |
